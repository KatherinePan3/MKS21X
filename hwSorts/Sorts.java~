public class Sorts{
    public static void SelectionSort(int[] num){
	int first;
	int temp;
	for(int i=num.length-1;i>0;i--){
	    first = 0;
	    for(k=1;k<=i;k++){
		if(num[k]<num[first]){
		    first=k;}
	    }
	    temp = num[first];
	    num[first]=num[i];
	    num[i]=temp;
	}
    }
